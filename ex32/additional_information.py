# make projects bro. You are creative. Everything will be done by you. 
# also do the scratch project. You are creative and everything will be fine.
# count = dict() or count = {}
# if title in counts:
#     counts[title] += 1 # checks for a key 
# else:
#     counts[title] = 1
"""Also always remember that you can do subarrays by that notation: if array is an array, array[i] is the i-th elements, 
than array[i:i+4] is a subarray from the i to the i+4; array[:i] means all the elements before i, also as array[i:]. Develop, bro"""
